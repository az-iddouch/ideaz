<div class="ideas-edit">
  <h2 class="ideas-add__header">Edit Idea</h2>
  <form action="/update/{{idea._id}}" method="POST" class="ideas-add__form">
    <label for="title" class="ideas-add__label ideas-add__label--title">Title</label>
    <input type="text" id="title" name="title" value="{{idea.title}}" class="ideas-add__input ideas-add__input--title">
    {{#if errors}}
    <div class="form-error">{{errors.title}}</div>
    {{/if}}

    <div class="form-row">

      {{#each categories}}
      <div class="ideas-add__categorie">
        <input type="radio" name="categorie" value={{_id}} class="ideas-add__categorie-radio" id="{{text}}"
          {{#checkEquals _id ../idea.categorie._id }} checked {{/checkEquals}}>
        <label for="{{text}}" class="ideas-add__categorie-label">{{text}}</label>
      </div>
      {{/each}}

    </div>

    <label for="body" class="ideas-add__label ideas-add__label--body">Body</label>
    <textarea name="body" id="body" class="ideas-add__input ideas-add__input--body" cols="30" rows="8">{{idea.body}}</textarea>
    {{#if errors}}
    <div class="form-error">{{errors.body}}</div>
    {{/if}}
    <input type="submit" class="ideas-add__form-submit" value="Update">
  </form>
</div>